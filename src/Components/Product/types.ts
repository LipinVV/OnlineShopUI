// описываем типы продукта

export type productType = {
    title: string,
    price: number,
    previewUrl: string,
    rating: number,
    salesCount: number,
    discount: number,
    priceNoDiscount: number,
    isFavourite: boolean,
}